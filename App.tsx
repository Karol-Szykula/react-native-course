import React from "react";
import { StyleSheet, View, Text } from "react-native";

export default function App() {
  return <Text>New App</Text>;
}

const styles = StyleSheet.create({
  screen: {
    padding: 50,
  },
});
